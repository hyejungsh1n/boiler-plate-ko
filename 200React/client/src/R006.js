import React, { useEffect } from 'react' 

// useEffect import : 함수형에서도 생명주기 메소드를 사용할 수 있다. 
// Component가 mount(처음 나타남), unmount(사라짐), update(상태가 바뀔 때) 특정 작업을 처리.

function R006(props) {

 useEffect(()=> {
     
 })


  return (
    <div>R006</div>
  )
}

export default R006